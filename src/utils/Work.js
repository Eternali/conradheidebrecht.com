'use strict';

class Work {
  constructor(institution, title, desc, start, end) {
    this.institution = institution;
    this.title = title;
    this.desc = desc;
    this.start = start;
    this.end = end;
  }
}

export default Work;